[package]
name = "ai-core"
version = "0.1.0"
edition = "2021"

[features]
default = []
onnx = ["tract-onnx"]

[dependencies]
serde = { version = "1", features = ["derive"] }
log = "0.4"
tract-onnx = { version = "0.21", optional = true }
